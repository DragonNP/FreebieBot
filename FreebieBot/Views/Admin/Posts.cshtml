@model IEnumerable<FreebieBot.Models.Posts.Post>
@{
    ViewBag.Title = "Posts";
}
<table class="table">
    <tr><td>Id</td><td> Name</td><td> Url</td><td> Type</td><td> Time</td></tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Url</td>
            <td>@item.Type</td>
            <td>@item.DateTime</td>
        </tr>
    }
</table>